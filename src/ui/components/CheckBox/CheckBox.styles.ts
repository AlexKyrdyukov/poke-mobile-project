import reactNative from 'react-native';

const styles = () => reactNative.StyleSheet.create({
  componentContainer: {
    marginBottom: 20,
  },
  touchableContainer: {
    width: 40,
    height: 40,
  },
});

export default styles;
