import reactNative from 'react-native';

const style = reactNative.StyleSheet.create({
  sectionContainer: {
    flex: 1,
  },
  imageContainer: {
    width: 100,
    height: 100,
  },
});

export default style;
